/**
 * @jest-environment node
 * @ts-nocheck
 */
import { createMocks } from "node-mocks-http";

describe("/api/route", () => {
  // test("should return the right stirng", async () => {
  //   const { req, res } = createMocks({
  //     method: "GET",
  //   });
  //   await GET(req, res);
  //   expect(res._getStatusCode()).toBe(200);
  //   expect(JSON.parse(res._getData()).message).toBe("Sup");
  // });
});
